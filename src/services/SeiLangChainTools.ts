export const createSeiTools = () => {
  return [] as any[];
};

// Minimal tool registry to satisfy LangChainSeiAgent imports without heavy deps
// Extend with real Tool instances if needed

export const createSeiTools = () => {
  return [] as any[];
};

